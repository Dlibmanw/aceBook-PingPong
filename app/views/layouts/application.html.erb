<!DOCTYPE html>
<html>
  <head>
    <title>Acebook</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
      <div class="nav-bar nav">
        <a class="navbar-brand" href="/">Acebook</a>
        <a class="nav-item nav-link" href="/posts/new">New Post</a>
        <a class="nav-item nav-link" href="/photos">New Photo</a>
      </div>
      <div class="nav-bar nav"> 
        <a class="nav-item nav-link" href="/users/<%= current_user.id %>"><%= current_user.name %></a>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-item nav-link"%>
      </div>
    </nav>
    
 
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
